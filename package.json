{
  "name": "pk-user-town",
  "version": "0.2.0",
  "private": true,
  "description": "Vercel-ready Pakistan mobile user town with resident brain APIs.",
  "type": "module",
  "scripts": {
    "start": "node server/run.js",
    "check": "node --check lib/town-core.js && node --check api/[...path].js && node --check public/app.js",
    "vercel:dev": "vercel dev",
    "import:data": "python scripts/import_data.py --out data/processed"
  }
}
